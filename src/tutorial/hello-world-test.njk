---
title: Tutorial · Hello World test
key: tutorial
description: "Install the controller, register an endpoint and run the Hello World test."
---
<h1>Hello World test</h1>
<p>Assuming you read the <a class="text-accent" href="/tutorial/">Orcraft tutorial overview</a>, we can start developing a first test. Let's look at its code and review it.</p>

<div class="code-block-wrap">
  <button class="button button--ghost install__copy code-copy" type="button"
    data-copy='from __future__ import annotations

from orcraft import (
	OrcraftLogger as Logger,
	OrcraftTest,
	OrcraftTestConfig,
	OrcraftTestCase,
	OrcraftConfigField,
)


class TestHelloWorldConfig(OrcraftTestConfig):
	name = OrcraftConfigField.Text(default="Hello World")
	
	description = OrcraftConfigField.Text(
		default=(
			"This is a minimal implementation of an Orcraft Test.\n"
			"It demonstrates how to define a test config, inherit from OrcraftTest, and implement a single test case.\n"
			"When executed, the test logs a simple Hello World message and returns a passing test result."
		)
	)


class TestHelloWorld(OrcraftTest):
	def __init__(self, config: TestHelloWorldConfig | None = None) -> None:
		super().__init__(config or TestHelloWorldConfig())

	def _run_case(self, case: OrcraftTestCase):

		Logger.log("Hello World", color=Logger.Color.BLUE)

		return case.result


def main() -> None:
	test = TestHelloWorld()
	test.run()


if __name__ == "__main__":
	main()'
    aria-label="Copy code" title="Copy code">
    <span class="install__copy-idle">{% icon "copy" %}</span>
    <span class="install__copy-done">{% icon "check" %}</span>
  </button>
  <pre class="code-block"><code class="language-python">from __future__ import annotations

from orcraft import (
	OrcraftLogger as Logger,
	OrcraftTest,
	OrcraftTestConfig,
	OrcraftTestCase,
	OrcraftConfigField,
)


class TestHelloWorldConfig(OrcraftTestConfig):
	name = OrcraftConfigField.Text(default="Hello World")
	
	description = OrcraftConfigField.Text(
		default=(
			"This is a minimal implementation of an Orcraft Test.\n"
			"It demonstrates how to define a test config, inherit from OrcraftTest, and implement a single test case.\n"
			"When executed, the test logs a simple Hello World message and returns a passing test result."
		)
	)


class TestHelloWorld(OrcraftTest):
	def __init__(self, config: TestHelloWorldConfig | None = None) -> None:
		super().__init__(config or TestHelloWorldConfig())

	def _run_case(self, case: OrcraftTestCase):

		Logger.log("Hello World", color=Logger.Color.BLUE)

		return case.result


def main() -> None:
	test = TestHelloWorld()
	test.run()


if __name__ == "__main__":
	main()
</code></pre>
</div>
