---
title: Tutorial · Running a group
key: tutorial
description: "Configure and run every test in a test group."
---
{% import "partials/zoomable-image.njk" as media %}
<h1>Running a group</h1>
<p>We've run individual tests and reviewed every parameter type in the
  <a class="text-accent" href="/tutorial/basic-configuration-parameters-group/">Basic Configuration Parameters
  group</a>. Now we'll configure the group as one run and execute all nine tests together.</p>

<h2 id="reloading-the-session">Reloading the session</h2>
<p>For this demonstration, we'll first reload the session so every test starts from its source defaults. Select
  the session node, then click <strong class="text-danger">Reload</strong>:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/reloading-session.png", "Reload action for reloading all reloadable suites and top-level groups in the current Orcraft session from source", "large") }}
<p>The Reload action reloads <strong>every</strong> reloadable suite and top-level group in the current session,
  not only the item currently selected. We can use it here because our session contains only one added item - the
  Tutorial suite. In a normal session containing several items, we usually wouldn't reload the entire session
  just to prepare one group for a run.</p>

<h2 id="selecting-the-group">Selecting the group</h2>
<p>In the Test Explorer, select the <strong>Basic Configuration Parameters</strong> group rather than one of its
  child tests. The Configuration tab switches to a multi-test view: the tests appear in a tree on the left, and
  selecting a test opens its configuration on the right.</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/selecting-basic-config-params-group.png", "Basic Configuration Parameters group selected in the Orcraft Test Explorer, with the multi-test configuration table and Folder Parameter editor visible", "large") }}
<p>This is still one group run, but every test keeps its own fields, cases and planned values. Move through the
  test list to configure each one before starting the run. The table provides a quick overview of all test
  values, while the editor below it lets us configure the selected test.</p>
<p><strong>All action buttons at the bottom now apply to the entire group.</strong> For example,
  <strong class="text-accent">Update Plan</strong> saves the configuration of every child test, and
  <strong>Reset To Defaults</strong> resets every child test - not only the test currently selected in the editor.
  The same group-wide scope applies to <strong>Export for CI</strong> and <strong>Run</strong>. Selecting a child in
  the table only chooses which test's fields are shown for editing.</p>

<h2 id="configuring-the-tests">Configuring the tests</h2>
<p>Every child test must have valid values. Fields marked as required can't be left empty:</p>
<ul>
  <li>Set <strong>Maze Executable Folder</strong> for Folder, Boolean, Selector, File, File List, Numeric,
    Multi-Selector and String Parameter.</li>
  <li>Add at least one entry to <strong>Maze Executable Folders</strong> for Folder List Parameter.</li>
  <li>Select a saved maze file for <strong>Maze Save/Load File</strong> in File Parameter.</li>
  <li>Select one or more saved maze files for <strong>Maze Save/Load Files</strong> in File List Parameter.</li>
</ul>
<p>The other fields already have usable defaults. You can leave them unchanged for the first group run, or vary
  them to see how each test turns its selected values into a Maze application command.</p>

<h3 id="shared-values">Setting repeated values</h3>
<p>Several tests need the same <strong>Maze Executable Folder</strong>. Start with Folder Parameter, click
  <strong>Browse</strong> and select the Maze application's <code>v3.0.0</code> folder. The selected value appears
  in both the test editor and its row in the overview table:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/configure-one-test-folder.png", "Folder Parameter configured with the Maze v3.0.0 executable folder in the group configuration", "large") }}
<p>Instead of entering the same folder separately for every test, use the value from that table cell. Hover over
  the configured <strong>Maze Executable Folder</strong> value and select
  <strong>Apply this value to all tests</strong>:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/apply-to-all-tests-menu.png", "Apply this value to all tests action shown for the configured Maze Executable Folder table cell", "large") }}
<p>The folder is copied to every test with the same <strong>Maze Executable Folder</strong> field:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/apply-clicked-on-folder.png", "Group configuration after applying the Maze Executable Folder value to all tests with that field", "large") }}
<p>Folder List Parameter remains empty because it declares the separate <strong>Maze Executable Folders</strong>
  list field. Configure that test independently by adding one or more Maze application folders.</p>

<h3 id="updating-the-plan">Updating the plan</h3>
<p>Changed values are shown as unplanned until they are added to the run plan. If we click
  <strong class="text-accent">Update Plan</strong> now, Orcraft validates the whole group and stops because three
  required fields are still empty:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/updating-plan-partially-config.png", "Missing Arguments dialog after trying to update the partially configured Basic Configuration Parameters group plan", "large") }}
<p>The dialog lists <strong>Maze Executable Folders</strong> for Folder List Parameter,
  <strong>Maze Save/Load File</strong> for File Parameter and <strong>Maze Save/Load Files</strong> for File List
  Parameter. No values are committed to the plan, and the existing plan remains unchanged. After we close the
  dialog, Orcraft takes us to the first missing field so we can continue configuring the group.</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/clicking-ok-next-missing.png", "Folder List Parameter and its missing Maze Executable Folders field revealed after closing the Missing Arguments dialog", "large") }}
<p>Clicking <strong>OK</strong> selects Folder List Parameter, scrolls the overview table to the missing
  <strong>Maze Executable Folders</strong> column and reveals the field in the editor. Add one or more Maze
  application folders here, then repeat the update attempt to find and configure any remaining missing
  arguments.</p>
<p>After configuring the folder list and the required file fields, every test is valid. The orange diamonds and
  purple values show that these changes are complete but still different from the current plan:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/all-configured-unplanned.png", "Fully configured Basic Configuration Parameters group with unplanned values marked by orange diamonds", "large") }}
<p>Select <strong class="text-accent">Update Plan</strong> again. Validation now passes, so Orcraft asks us to
  confirm that the current configuration should overwrite the existing planned values:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/update-plan-confirmation-dialog.png", "Confirmation dialog asking whether to apply the current group configuration as the new plan", "large") }}
<p>Select <strong>Yes</strong> to update the plan for every test in the group. The values turn blue, the orange
  diamonds disappear and <strong class="text-accent">Update Plan</strong> becomes disabled because the current
  configuration now matches the plan:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/updated-plan.png", "Basic Configuration Parameters group after all current values have been saved to the plan", "large") }}
<p>We could also run the group with unplanned values, just as we did with an individual test, but every required
  field would still need to be complete.</p>

<h3 id="test-configuration">Test configuration is preserved</h3>
<p>The group configuration isn't a separate copy. Every value configured here is also available when we select
  that child test for an individual run. While we're editing the group, those values are preserved as the test's
  draft configuration; after <strong class="text-accent">Update Plan</strong>, they become that test's planned
  values as well. There is no need to configure the test again.</p>
<p>For example, selecting Folder List Parameter on its own still shows the three Maze application folders we set
  while configuring the group:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/selected-test-keeps-configs.png", "Standalone Folder List Parameter configuration retaining the three folders configured through the group", "large") }}

<h2 id="running-the-group">Running the group</h2>
<p>Let's select the <strong>Basic Configuration Parameters</strong> group again and click <strong>Run</strong>.
  Even though we selected Folder List Parameter on its own in the previous step, every test's configuration is
  still present when we return to the group - changing the selection doesn't discard any values. Orcraft then
  confirms the complete scope before starting: in this configuration, 9 test cases across 9 tests.</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/running-confirmation.png", "Confirm Run dialog for running nine test cases across all nine tests in the Basic Configuration Parameters group", "large") }}
<p>Select <strong>Yes</strong> to continue. Orcraft validates every selected test before the run begins. If a
  required value is missing, the Missing Arguments dialog identifies the test and field that still need
  attention; return to that test in the configuration tree, set the value and run again.</p>
<p>Once validation passes, Orcraft creates a run for every child test and starts all nine in parallel. Their test
  run rows appear beneath the group, with live progress in the Status column and a <strong>Running 9</strong>
  summary above the tree:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/tests-started-running.png", "All nine Basic Configuration Parameters child test runs executing in parallel with live progress", "large") }}
<p>Each child keeps its own log and result, while the group node provides one place to follow the complete run.</p>

<h2 id="reading-the-results">Reading the results</h2>
<p>When execution finishes, the summary changes to <strong class="text-success">Pass 9</strong> and every child
  run shows its own <strong class="text-success">Pass</strong> status:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/tests-finished.png", "Completed Basic Configuration Parameters group with all nine child test runs passing", "large") }}
<p>Expand the completed group in the execution tree to inspect each test. Select a child result to read its
  command, captured Maze application output and final status.</p>

<h3 id="group-details">Group details</h3>
<p>The GUI doesn't assign a status to the group itself - running a group creates and runs its child tests, and
  each test run has its own status. To inspect the combined execution data, right-click the group and select
  <strong>Details</strong>:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/right-clicking-group-details.png", "Details action in the context menu of the completed Basic Configuration Parameters group", "large") }}
<p>The Details dialog brings the group configuration and child results together. On the left, each test compares
  its default, planned and run values. On the right, the results tree lists every test and cycle with its
  endpoint, status, timing and controller version, while the summary reports aggregate counts and progress:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/group-details-view.png", "Basic Configuration Parameters Details dialog showing test configurations, aggregate counts and the test and cycle result hierarchy", "large") }}
<p>This group contains one case and one cycle per test, so the hierarchy is still simple. The Details view becomes
  more useful later in the tutorial, when runs include multiple cases, flows and steps that can be inspected at
  their corresponding levels.</p>
<p>Details aren't limited to groups. Double-click any node in the execution tree - from the Session node through
  suites, groups and tests down to an individual test-run instance - to open the Details view for that scope.
  These details can also be exported to an XLSX workbook. We'll cover the deeper result hierarchy and XLSX export
  later in the tutorial.</p>

<h2 id="rerunning-and-clearing">Rerunning and clearing</h2>

<h3 id="rerunning-with-a-change">Rerunning with a change</h3>
<p>We can rerun the entire group with a changed value without updating the plan first. Here we change only Folder
  Parameter's <strong>Maze Executable Folder</strong> from <code>v3.0.0</code> to <code>v2.0.0</code>, leave the
  orange diamond in place and click <strong>Run</strong>:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/changing-and-running-unplanned.png", "Basic Configuration Parameters group ready to rerun with only Folder Parameter changed to the unplanned v2.0.0 folder", "large") }}
<p>Running the group creates a new run for every child test, even though only one value changed. The latest Folder
  Parameter run receives a <strong class="text-warning">Plan Mismatch</strong> badge because it used
  <code>v2.0.0</code> instead of its planned <code>v3.0.0</code>; the other eight new runs still match their
  plans:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/only-one-test-plan-mismatch.png", "Second Basic Configuration Parameters group run with only Folder Parameter marked as a plan mismatch", "large") }}
<p>The Details dialog makes the difference explicit by placing the planned and run values side by side. Folder
  Parameter's <code>v3.0.0</code> Plan Value and <code>v2.0.0</code> Run Value are
  <strong class="text-warning">highlighted</strong>, while the other
  tests retain matching values:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/plan-mismatch-in-details.png", "Group Details dialog comparing the planned v3.0.0 folder with Folder Parameter's unplanned v2.0.0 run value", "large") }}

<h3 id="cleaning-up-runs">Cleaning up superseded runs</h3>
<p>After repeated group runs, the execution tree can accumulate several run instances under every test. Select
  the group and choose <strong class="text-orange">Clean Up</strong> to remove all but the latest running or
  finished instance under each child:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/clean-up-option.png", "Clean Up action for retaining only the latest run instance under each test in the group", "large") }}
<p>The older, dimmed superseded runs disappear, leaving the latest run for each test. The Folder Parameter run
  keeps its Plan Mismatch badge because cleanup changes only which run instances are retained:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/clean-up-removed-superseeded.png", "Basic Configuration Parameters group after cleanup removed superseded runs and retained each latest run", "large") }}

<h3 id="clearing-runs">Clearing all run instances</h3>
<p>To remove every run instance beneath the group, including running, queued and completed runs, select
  <strong class="text-danger">Clear</strong>. This keeps the group and all of its child tests:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/clear-option.png", "Clear action for removing every run instance beneath the Basic Configuration Parameters group", "large") }}
<p>After clearing, no test-run rows remain, but the configuration is untouched. The unplanned
  <code>v2.0.0</code> Folder Parameter value is still present, along with the other test values, so clearing run
  history doesn't reset drafts or planned configuration:</p>
{{ media.zoomableImage("/assets/images/screenshots/tutorial/running-a-group/all-cleared-config-stays.png", "Basic Configuration Parameters group after all run instances were cleared while its test configuration remained", "large") }}