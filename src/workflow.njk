---
layout: layouts/base.njk
title: Workflow
key: workflow
description: How teams build tests, connect endpoints and run and analyze results with Orcraft.
---

<section class="section container">
  <h1>Workflow</h1>
  <p class="text-muted">From an empty folder to analyzed results in three steps.</p>

  <ol class="steps">
    <li class="step">
      <span class="card__icon">{% icon "code" %}</span>
      <h3 class="step__title">Generate and build</h3>
      <p class="step__text">Generate the suite, then refine flows and steps in Python.</p>
    </li>
    <li class="step">
      <span class="card__icon">{% icon "network" %}</span>
      <h3 class="step__title">Connect endpoints</h3>
      <p class="step__text">Register local, remote and shared endpoints and select those required for the run.</p>
    </li>
    <li class="step">
      <span class="card__icon">{% icon "player-play" %}</span>
      <h3 class="step__title">Run and analyze</h3>
      <p class="step__text">Launch the session, monitor progress case by case, then review logs, results and reports.</p>
    </li>
  </ol>
</section>
